require('./fonts.css');
require('./normalize.css');
require('./reset.less');
require('./common.less');

// Код для расчёта высоты мобильного меню, учитывающего
// интерфейсы управления браузера
// let vh = window.innerHeight * 0.01;
//
// $(':root').css('--vh', `${vh}px`);
//
// $(window).on('resize', () => {
//     let vh = window.innerHeight * 0.01;
//     $(':root').css('--vh', `${vh}px`);
// });